name: CI

on:
  push:
    branches: ["**"]
    tags: ["v*"]
  pull_request:

jobs:
  ci:
    uses: https://harton.dev/james/workflows/.forgejo/workflows/elixir-lib.yml@main
    runs-on: docker
    with:
      git_ops_enabled: false
      hex_release_enabled: true
      extra_packages: "libusb-1.0-0-dev"
    secrets: inherit
